package ec504Group3.Checker;

public interface checker {
    /**
     * @argument File address of file needed to check
     * @return Score for specific  File
     */
    int check(String checkFile);
}
